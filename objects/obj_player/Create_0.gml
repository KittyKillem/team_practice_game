move_speed = 1;
hp_current = hp_max;
facing = 0

function add_xp(_xp_to_add)
{
	if (xp_current >= xp_max)
	{
		xp_current -= xp_max
		strength += 0.5
		hp_current = hp_max
	}	
}

image_blend = c_red
tilemap_to_collide = layer_tilemap_get_id("Tiles_Col");